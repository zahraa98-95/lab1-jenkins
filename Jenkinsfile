pipeline {
    agent any
    stages {
        stage('Execute shell command') {
            steps {
                sh '''
                echo "Hello, World!"
                '''
            }
        }
    }
}
